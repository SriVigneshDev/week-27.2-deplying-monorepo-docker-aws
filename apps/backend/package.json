{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc -b",
    "build:bundle": "node esbuild.config.mjs",
    "build:prod": "npm run build && npm run build:bundle",
    "start": "node dist/index.js",
    "start:prod": "node standalone.js",
    "dev": "npm run build && npm run start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "6.15.0",
    "@repo/typescript-config": "workspace:*",
    "@types/express": "^5.0.3",
    "db": "workspace:*",
    "express": "^5.1.0",
    "prisma": "^6.15.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.10"
  }
}
